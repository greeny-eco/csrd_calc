name: csrd_calc
description: "CSRD Date Calculator"

version: 1.1.1+10101
publish_to: 'none'

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  page_transition: ^2.1.0
  url_launcher: ^6.2.6
  loading_indicator: ^3.1.1
  flutter_localizations:
    sdk: flutter
  intl: any

  # Firebase
  firebase_core: ^2.30.1
  firebase_analytics: ^10.10.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  generate: true

  assets:
    - assets/images/
